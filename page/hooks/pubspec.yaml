name: page_hooks

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  template_utils:
      git:
        url: https://github.com/andrzejchm/flutter-clean-arch-templates.git
        path: template_utils
        ref: 'main'
  mason: any
  recase: 4.0.0